# Minimal requirements for maximum compatibility
# Use these if the main requirements fail

# Core web framework
fastapi==0.95.0
uvicorn[standard]==0.20.0

# Data validation (older stable version)
pydantic!=1.8

# Google Cloud (older stable versions)
google-cloud-pubsub==2.10.0
google-auth==2.15.0
google-auth-oauthlib==0.8.0
google-auth-httplib2==0.1.0

# Testing (minimal versions)
pytest==7.2.0
pytest-asyncio==0.19.0
pytest-cov==4.0.0

# HTTP client
httpx==0.23.0

# Multipart form handling
python-multipart==0.0.4
